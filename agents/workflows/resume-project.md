# Workflow: /resume-project

1. Read `.planning/PROJECT_STATE.json` to identify current phase and gate.
2. Read `.planning/MASTER_CHECKLIST.md` to identify pending tasks.
3. Run `python scripts/run_all.py` to verify environment readiness.
